namespace HelloWorld {
    val main = fun() {
        console.log({});
        console.log({"Hello, world!";});
        console.log({"ignored"; "Wello, horld!";});
    };
}

val main = HelloWorld.main;
